#ifndef _UTIL_
#define _UTIL_

#include <opencv2/opencv.hpp>
#include "iostream"


//namespace Utils
//

//class Utils
//{
//
//public:
//	static const bool DUMPOUTPUTS = false;
//
//	static const bool LEANEXECUTION = false; //flag to run only algorithm and nothing else
//
//	//static double GetPSNR(const cv::Mat& I1, const cv::Mat& I2);
//	//static double GetSSIM(const cv::Mat& i1, const cv::Mat& i2);
//};


	//inline double GetPSNR(const cv::Mat& I1, const cv::Mat& I2);
	//inline double GetSSIM(const cv::Mat& i1, const cv::Mat& i2);



//class Utils
//{
//private:
//
//public:
//	double GetPSNR(const Mat& I1, const Mat& I2);
//	Scalar GetSSIM(const Mat& i1, const Mat& i2);
//};
//#include <Windows.h>
//#include <vector>
//
//using namespace std;
//
//
////Simple struct to return from lsfiles
//struct List {
//	vector<string> files;
//	vector<string> folders;
//};
//
//List lsfiles(string folder);
#endif